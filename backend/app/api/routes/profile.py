"""Profile routes: view the verified data store; delete profile + docs + history (cascade)."""

from fastapi import APIRouter

router = APIRouter()

# TODO: GET /, DELETE / (cascade delete — first-class data-minimization feature)
