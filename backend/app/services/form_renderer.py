"""Render the approved field values into a downloadable filled form (PDF / structured output).

Produces the final draft the user downloads and submits themselves. NEVER submits
to any external portal.
"""

# TODO: render(form_type, approved_fields) -> bytes (PDF)
