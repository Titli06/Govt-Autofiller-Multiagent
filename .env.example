# Copy to .env and fill in. NEVER commit the real .env.

# --- App ---
ENV=development
API_HOST=0.0.0.0
API_PORT=8000

# --- Auth ---
JWT_SECRET=change-me-to-a-long-random-string
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60

# --- Field-level PII encryption (32-byte key, base64) ---
PII_ENCRYPTION_KEY=change-me-base64-32-bytes

# --- PostgreSQL ---
DATABASE_URL=postgresql+psycopg://govfill:govfill@postgres:5432/govfill

# --- Redis / Celery ---
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1

# --- Object storage (S3-compatible: MinIO in dev, S3 in prod) ---
S3_ENDPOINT_URL=http://minio:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=govfill-documents
S3_REGION=us-east-1

# --- Vision-LLM (primary OCR/extraction) ---
ANTHROPIC_API_KEY=
VISION_MODEL=claude-opus-4-8

# --- Confidence policy ---
CONFIDENCE_THRESHOLD=0.90
