// Doc Upload page: drag-drop + camera capture for ID documents (UC1) and blank forms.
// Kicks off async OCR/fill jobs and polls their status. — Placeholder until Phase 1.

export default function Upload() {
  return (
    <div>
      <h1>Upload</h1>
      <p>Document upload arrives in Phase 1.</p>
    </div>
  );
}
