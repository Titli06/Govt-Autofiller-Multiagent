// Review/Edit screen: confidence-coded fields (green = auto-filled/verified,
// yellow = low confidence, red = missing/high-stakes). One-click approve/edit per
// field, side-by-side with the source document. Download stays DISABLED until every
// flagged field is resolved. — Placeholder until Phase 3.

export default function Review() {
  return (
    <div>
      <h1>Review</h1>
      <p>Form review arrives in Phase 3.</p>
    </div>
  );
}
